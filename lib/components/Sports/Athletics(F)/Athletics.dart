import 'package:flutter/material.dart';
import 'home.dart';

void main() => runApp(Athletics_F());

class Athletics_F extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Home(),
    );
  }
}
