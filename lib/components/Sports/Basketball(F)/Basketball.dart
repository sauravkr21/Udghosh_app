import 'package:flutter/material.dart';
import 'home.dart';

void main() => runApp(Basketball_F());

class Basketball_F extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Home(),
    );
  }
}
