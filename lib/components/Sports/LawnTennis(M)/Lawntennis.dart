import 'package:flutter/material.dart';
import 'home.dart';

void main() => runApp(LawnTennis_M());

class LawnTennis_M extends StatelessWidget {
@override
Widget build(BuildContext context) {
return MaterialApp(
debugShowCheckedModeBanner: false,
home: Home(),
);
}
}
