import 'package:flutter/material.dart';
import 'home.dart';

void main() => runApp(TableTennis_M());

class TableTennis_M extends StatelessWidget {
@override
Widget build(BuildContext context) {
return MaterialApp(
debugShowCheckedModeBanner: false,
home: Home(),
);
}
}
